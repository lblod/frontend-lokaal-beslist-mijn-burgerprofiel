{{page-title "Lokaal Beslist"}}
<AuApp>
  <AuModalContainer />

  {{! template-lint-disable no-inline-styles }}
  <AuMainContainer style="max-height: 100vh;" as |m|>
    <m.content @scroll={{true}}>
      <p>DeviceId: {{this.mbpEmbed.deviceId}}</p>
      {{outlet}}
    </m.content>
  </AuMainContainer>
</AuApp>