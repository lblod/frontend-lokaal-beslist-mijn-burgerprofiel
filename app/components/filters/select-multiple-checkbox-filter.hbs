<Filters::Filter @id={{@id}} @label={{@label}} @labelHidden={{@labelHidden}}>
  <AuCheckboxGroup
    @disabled={{@disabled}}
    @selected={{this.selected}}
    @onChange={{this.selectChange}}
    as |Group|
  >
    {{#each @options as |option|}}
      <Group.Checkbox @value={{option}}>{{option.label}}</Group.Checkbox>
    {{/each}}
  </AuCheckboxGroup>
</Filters::Filter>