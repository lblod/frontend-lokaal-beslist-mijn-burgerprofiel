<AuTabs as |Tab|>
  <Tab>
    <AuLink
      @route="agenda-items.index"
      @query={{this.filters}}
      class={{if
        (eq this.router.currentRouteName "agenda-items.index")
        "active"
      }}
    >
      Agendapunten</AuLink>
  </Tab>
  <Tab>
    <AuLink
      @route="sessions.index"
      @query={{this.filters}}
      class={{if (eq this.router.currentRouteName "sessions.index") "active"}}
    >
      Zittingen</AuLink>
  </Tab>
</AuTabs>